// import { instance } from "../redux/api/instance";

// // const setToken = (accessToken) => {
// //   accessToken = instance.defaults.headers.common["setToken"] = accessToken;
// // };

// const setToken = (accessToken, userkey) => {
//   accessToken = instance.defaults.headers.common[
//     "Cookie"
//   ] = `accessToken=${accessToken} userkey=${userkey}`;
//   // userkey = instance.defaults.headers.common["userkey"] = `userkey=${userkey}`;
// };
// export default setToken;

// // import { instance } from "../redux/api/instance";

// // const setToken = (accessToken) => {
// //   if (accessToken) {
// //     instance.defaults.headers.common["authorization"] = `Bearer ${accessToken}`;
// //   } else {
// //     delete instance.defaults.headers.common["authorization"];
// //   }
// // };

// // export default setToken;
